import { createTheme } from "@mui/material";

export  const theme = createTheme({
    palette: {
      primary: {
        main: '#c0ca33', // Utilisation de la teinte principale de la palette pour les éléments principaux
      },
      secondary: {
        main: '#d4e157', // Utilisation de la teinte principale de la palette pour les éléments secondaires
      },
    },
  })